---
phase: 01-baseline-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/runtime-context-map.md
  - docs/architecture/behavior-baseline.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
must_haves:
  truths:
    - "维护者能直接看到 background、content、sidepanel、offscreen、worker 各自负责什么。"
    - "五种模式、快捷翻译、全文抓取、设置恢复这些现有链路被明确写成不可回归的现状。"
    - "消息名、存储键和关键触发链路不再只藏在源码里。"
  artifacts:
    - path: "docs/architecture/runtime-context-map.md"
      provides: "五个扩展上下文的职责、消息方向、存储键和入口文件地图"
      min_lines: 80
    - path: "docs/architecture/behavior-baseline.md"
      provides: "后续重构必须保留的关键用户行为与失败保护线"
      min_lines: 60
  key_links:
    - from: "docs/architecture/runtime-context-map.md"
      to: "entrypoints/background.ts"
      via: "背景脚本职责与消息来源说明"
      pattern: "background.ts"
    - from: "docs/architecture/runtime-context-map.md"
      to: "entrypoints/content.ts"
      via: "快捷翻译和页面抓取链路说明"
      pattern: "QUICK_TRANSLATE|GET_PAGE_CONTENT|OPEN_SIDE_PANEL"
    - from: "docs/architecture/behavior-baseline.md"
      to: "entrypoints/sidepanel/hooks/useSettings.ts"
      via: "设置恢复和存储兼容说明"
      pattern: "selectedText|menuIntentMode|fetchPageIntent|autoTriggerAt"
---

<objective>
冻结当前扩展的真实运行边界和关键用户行为，把“系统现在怎么工作”写成后续阶段都能直接沿用的基线。

Purpose: 先把现状讲清楚，避免后续重构时凭印象改坏现有流程。
Output: 一份上下文职责地图和一份行为保护线文档。
</objective>

<execution_context>
@/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-and-contracts/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@entrypoints/background.ts
@entrypoints/content.ts
@entrypoints/offscreen/main.ts
@entrypoints/sidepanel/App.tsx
@entrypoints/sidepanel/hooks/useSettings.ts
@entrypoints/sidepanel/hooks/useWorker.ts
@entrypoints/sidepanel/types/index.ts

<interfaces>
From entrypoints/sidepanel/types/index.ts:
```typescript
export type ModeKey = 'summarize' | 'correct' | 'proofread' | 'translate' | 'expand';
export interface Settings { engine: EngineType; extensionLanguage: string; tone: string; detailLevel: string; localModel: string; apiBaseUrl: string; apiKey: string; apiModel: string; autoSpeak: boolean; translateFallback?: TranslateFallbackProvider; readyConfigs?: string[]; failedConfigs?: string[]; }
export interface PageContentMessage { type: 'GET_PAGE_CONTENT'; }
export interface QuickTranslateMessage { type: 'QUICK_TRANSLATE'; text?: string; }
export interface OpenSidePanelMessage { type: 'OPEN_SIDE_PANEL'; }
export interface WorkerUpdateBridgeMessage { type: 'WORKER_UPDATE'; data: WorkerOutboundMessage; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写出五个扩展上下文的职责地图</name>
  <read_first>
    - docs/architecture/runtime-context-map.md
    - .planning/phases/01-baseline-and-contracts/01-RESEARCH.md
    - .planning/codebase/ARCHITECTURE.md
    - entrypoints/background.ts
    - entrypoints/content.ts
    - entrypoints/offscreen/main.ts
    - entrypoints/sidepanel/App.tsx
    - entrypoints/sidepanel/hooks/useWorker.ts
    - entrypoints/sidepanel/types/index.ts
  </read_first>
  <files>docs/architecture/runtime-context-map.md</files>
  <action>新建或重写 `docs/architecture/runtime-context-map.md`，按 ARCH-01 和 ARCH-03 明确写出 `background.ts`、`content.ts`、`sidepanel/App.tsx`、`offscreen/main.ts`、`sidepanel/worker.ts` 五个上下文的职责、标准消息链路、Chrome AI 旁路、关键存储键、入口文件、当前已知限制。文档中必须逐字列出这些消息名：`OPEN_SIDE_PANEL`、`GET_PAGE_CONTENT`、`QUICK_TRANSLATE`、`INIT_ENGINE`、`GENERATE`、`RESET_ENGINE`、`WORKER_UPDATE`；必须逐字列出这些存储键：`selectedText`、`settings`、`activeTab`、`engineStatus`、`menuIntentMode`、`fetchPageIntent`、`lastProgress`、`autoTriggerAt`、`engineError`、`isLocalModelEnabled`。不要发明新架构，不要把 Chrome AI 误写成经由 offscreen 的标准路径。</action>
  <acceptance_criteria>
    - `docs/architecture/runtime-context-map.md` 存在并且同时出现五个入口文件名。
    - 文档中能 grep 到 7 个消息名和 10 个存储键的原文。
    - 文档明确写出 “Chrome AI” 和 “旁路/特例” 语义，而不是把它并入 offscreen 标准路径。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "background.ts|content.ts|App.tsx|offscreen/main.ts|worker.ts|OPEN_SIDE_PANEL|GET_PAGE_CONTENT|QUICK_TRANSLATE|INIT_ENGINE|GENERATE|RESET_ENGINE|WORKER_UPDATE|selectedText|settings|activeTab|engineStatus|menuIntentMode|fetchPageIntent|lastProgress|autoTriggerAt|engineError|isLocalModelEnabled|Chrome AI" docs/architecture/runtime-context-map.md</automated>
  </verify>
  <done>维护者打开一份文档就能看清五个上下文分别做什么、如何互相发消息、哪些存储键支撑当前行为。</done>
</task>

<task type="auto">
  <name>Task 2: 写出不可回归的行为保护线</name>
  <read_first>
    - docs/architecture/behavior-baseline.md
    - .planning/phases/01-baseline-and-contracts/01-RESEARCH.md
    - entrypoints/background.ts
    - entrypoints/content.ts
    - entrypoints/sidepanel/App.tsx
    - entrypoints/sidepanel/hooks/useSettings.ts
    - entrypoints/sidepanel/hooks/useWorker.ts
  </read_first>
  <files>docs/architecture/behavior-baseline.md</files>
  <action>新建或重写 `docs/architecture/behavior-baseline.md`，按 ARCH-03 列出后续阶段必须保留的关键行为：五种模式可继续触发、快捷翻译依赖 `selectedText -> QUICK_TRANSLATE -> WORKER_UPDATE` 链路、全文抓取依赖 `GET_PAGE_CONTENT`、右键菜单全文翻译依赖 `menuIntentMode=translate + fetchPageIntent=true + autoTriggerAt`、设置持久化继续复用本地与会话存储分工、在线模式 API Key 不长期落盘。每条行为都写出触发入口、依赖的消息/存储键、成功结果、容易回归的风险点。</action>
  <acceptance_criteria>
    - 文档至少覆盖五种模式、快捷翻译、全文抓取、设置恢复、在线 API Key 持久化策略。
    - 文档中能 grep 到 `selectedText`、`QUICK_TRANSLATE`、`WORKER_UPDATE`、`GET_PAGE_CONTENT`、`menuIntentMode`、`fetchPageIntent`、`autoTriggerAt`、`storage.session`。
    - 每个关键行为都写明“触发入口”和“成功结果”。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "summarize|correct|proofread|translate|expand|selectedText|QUICK_TRANSLATE|WORKER_UPDATE|GET_PAGE_CONTENT|menuIntentMode|fetchPageIntent|autoTriggerAt|storage.session|触发入口|成功结果" docs/architecture/behavior-baseline.md</automated>
  </verify>
  <done>后续执行者能从文档直接知道哪些现有用户流程绝不能改坏，以及每条流程依赖哪些消息和存储键。</done>
</task>

</tasks>

<verification>
检查两份文档是否真实引用当前源码中的消息名、存储键和入口文件，而不是抽象概述。
</verification>

<success_criteria>
`docs/architecture/runtime-context-map.md` 和 `docs/architecture/behavior-baseline.md` 可以作为 Phase 2-4 的行为和边界真源。
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-contracts/01-01-SUMMARY.md`
</output>
