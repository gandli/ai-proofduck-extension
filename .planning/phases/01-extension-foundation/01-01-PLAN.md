---
phase: 01-extension-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - wxt.config.ts
  - tailwind.config.js
  - postcss.config.js
  - public
  - entrypoints/background.ts
  - entrypoints/sidepanel/index.html
  - entrypoints/sidepanel/main.tsx
  - entrypoints/sidepanel/globals.css
autonomous: true
requirements:
  - CORE-01
  - QUAL-01
must_haves:
  truths:
    - "仓库重新具备可运行的浏览器扩展基础骨架。"
    - "侧边栏入口可以被构建出来。"
    - "技术栈明确落到 WXT + React + TypeScript + Tailwind。"
---

<objective>
建立最小可运行的浏览器扩展骨架，让项目重新具备开发起点。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 初始化扩展工程骨架</name>
  <files>package.json, tsconfig.json, wxt.config.ts, tailwind.config.js, postcss.config.js, public/*</files>
  <action>创建新的 WXT + React + TypeScript + Tailwind 项目基础文件，包括依赖、脚本、构建配置、类型检查配置和最小图标/多语言资源占位。不要实现业务逻辑，只保证扩展工程能构建。</action>
  <acceptance_criteria>
    - 存在 `package.json`，包含 `dev`、`build`、`compile`、`test`
    - 存在 `wxt.config.ts`
    - 存在 `entrypoints/sidepanel/index.html` 与 `entrypoints/sidepanel/main.tsx`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 建立最小后台与侧边栏入口</name>
  <files>entrypoints/background.ts, entrypoints/sidepanel/index.html, entrypoints/sidepanel/main.tsx, entrypoints/sidepanel/globals.css</files>
  <action>补齐 background 和 sidepanel 的最小入口，让浏览器扩展加载后至少可以打开一个带基础样式的侧边栏页面。</action>
  <acceptance_criteria>
    - 扩展包含 background 入口
    - sidepanel 页面能挂载 React 根组件
    - 页面有基础全局样式
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
仓库恢复为一个可继续开发的现代浏览器扩展工程，而不是只有规划文档的空目录。
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-foundation/01-01-SUMMARY.md`
</output>
