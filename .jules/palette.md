## 2024-06-12 - Translation Textarea Clear Button
**Learning:** Adding interactive, absolutely positioned elements (like a clear button) inside input or textarea containers often causes text to flow underneath and overlap the button, reducing readability.
**Action:** Always add sufficient right padding (e.g., `pr-10`) to the parent input or textarea element's classes to reserve space for the absolutely positioned floating elements.
