---
phase: 01-extension-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - entrypoints/sidepanel/App.tsx
  - entrypoints/sidepanel/components/*
  - entrypoints/sidepanel/types/index.ts
  - entrypoints/sidepanel/hooks/useSettings.ts
  - entrypoints/shared/contracts.ts
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - INPT-03
  - SET-01
  - SET-03
must_haves:
  truths:
    - "侧边栏里能看到五种模式、输入区、结果区和状态区。"
    - "用户可以直接在侧边栏输入文本。"
    - "基础设置可以保存并恢复。"
---

<objective>
把侧边栏从空壳推进到可交互的基础界面，并建立基础设置恢复能力。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 建立侧边栏主界面壳子</name>
  <files>entrypoints/sidepanel/App.tsx, entrypoints/sidepanel/components/*, entrypoints/sidepanel/types/index.ts</files>
  <action>实现一个最小但完整的侧边栏主界面，至少包含五种模式切换、原文输入区、结果区、状态显示和设置入口。模式先允许空实现，但界面结构和状态流要成立。</action>
  <acceptance_criteria>
    - 五种模式都可见并可切换
    - 有文本输入区
    - 有结果显示区
    - 有状态区域或按钮反馈
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 建立基础设置与恢复</name>
  <files>entrypoints/sidepanel/hooks/useSettings.ts, entrypoints/shared/contracts.ts</files>
  <action>实现基础设置对象与存储逻辑，至少覆盖目标语言、主要引擎、基础默认值和恢复能力。先以低风险的本地存储恢复为主，不接复杂引擎执行。</action>
  <acceptance_criteria>
    - 设置有默认值
    - 用户修改设置后可以恢复
    - 相关类型和默认值有统一入口
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
用户打开扩展时，已经能看到“像一个产品”的侧边栏，而不是只有脚手架。
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-foundation/01-02-SUMMARY.md`
</output>
