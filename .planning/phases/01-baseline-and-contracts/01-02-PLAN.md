---
phase: 01-baseline-and-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - entrypoints/shared/contracts.ts
  - entrypoints/shared/storage.ts
  - entrypoints/background.ts
  - entrypoints/content.ts
  - entrypoints/offscreen/main.ts
  - entrypoints/sidepanel/hooks/useSettings.ts
  - entrypoints/sidepanel/hooks/useWorker.ts
  - README.md
autonomous: true
requirements:
  - ARCH-02
  - QUAL-03
must_haves:
  truths:
    - "模式、消息协议和存储键有一个明确的共享入口供多上下文复用。"
    - "background、content、offscreen、sidepanel 不再各自维护漂移的消息名或模式列表。"
    - "README 对真实产物目录、入口职责和测试入口的描述与仓库一致。"
  artifacts:
    - path: "entrypoints/shared/contracts.ts"
      provides: "跨上下文共享的模式、设置和消息协议入口"
      min_lines: 20
    - path: "entrypoints/shared/storage.ts"
      provides: "跨上下文共享的存储键常量"
      min_lines: 20
    - path: "README.md"
      provides: "与真实架构、产物目录和测试命令一致的项目说明"
      min_lines: 120
  key_links:
    - from: "entrypoints/background.ts"
      to: "entrypoints/shared/contracts.ts"
      via: "统一模式和消息类型引用"
      pattern: "from './shared/contracts'"
    - from: "entrypoints/content.ts"
      to: "entrypoints/shared/storage.ts"
      via: "共享存储键常量引用"
      pattern: "from './shared/storage'"
    - from: "README.md"
      to: "wxt.config.ts"
      via: "构建输出目录说明"
      pattern: "dist"
---

<objective>
把分散的模式、消息名、存储键和说明文档收拢到稳定的共享入口，同时修正 README 与真实仓库行为的漂移。

Purpose: 给后续重构提供统一契约，减少重复定义和文档误导。
Output: 共享契约入口、共享存储键入口，以及对齐后的 README。
</objective>

<execution_context>
@/Users/user/.codex/get-shit-done/workflows/execute-plan.md
@/Users/user/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-and-contracts/01-RESEARCH.md
@docs/architecture/runtime-context-map.md
@docs/architecture/behavior-baseline.md
@README.md
@wxt.config.ts
@package.json
@entrypoints/sidepanel/types/index.ts
@entrypoints/background.ts
@entrypoints/content.ts
@entrypoints/offscreen/main.ts
@entrypoints/sidepanel/hooks/useSettings.ts
@entrypoints/sidepanel/hooks/useWorker.ts

<interfaces>
From entrypoints/sidepanel/types/index.ts:
```typescript
export type ModeKey = 'summarize' | 'correct' | 'proofread' | 'translate' | 'expand';
export const MODES: ModeDefinition[];
export interface Settings { ... }
export interface QuickTranslateMessage { type: 'QUICK_TRANSLATE'; text?: string; }
export interface InitEngineMessage { type: 'INIT_ENGINE'; settings: Settings; }
export interface GenerateMessage { type: 'GENERATE'; text: string; mode: ModeKey; settings: Settings; }
export interface ResetEngineMessage { type: 'RESET_ENGINE'; }
export interface WorkerUpdateBridgeMessage { type: 'WORKER_UPDATE'; data: WorkerOutboundMessage; }
```

From package.json:
```json
{
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test"
  }
}
```

From wxt.config.ts:
```typescript
export default defineConfig({
  outDir: 'dist'
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立跨上下文共享契约入口并替换重复定义</name>
  <read_first>
    - entrypoints/shared/contracts.ts
    - entrypoints/shared/storage.ts
    - entrypoints/sidepanel/types/index.ts
    - entrypoints/background.ts
    - entrypoints/content.ts
    - entrypoints/offscreen/main.ts
    - entrypoints/sidepanel/hooks/useSettings.ts
    - entrypoints/sidepanel/hooks/useWorker.ts
    - docs/architecture/runtime-context-map.md
  </read_first>
  <files>entrypoints/shared/contracts.ts, entrypoints/shared/storage.ts, entrypoints/background.ts, entrypoints/content.ts, entrypoints/offscreen/main.ts, entrypoints/sidepanel/hooks/useSettings.ts, entrypoints/sidepanel/hooks/useWorker.ts</files>
  <action>按 ARCH-02 建立 `entrypoints/shared/contracts.ts` 和 `entrypoints/shared/storage.ts`。保持 `entrypoints/sidepanel/types/index.ts` 作为低风险真源，但通过 `entrypoints/shared/contracts.ts` 明确对外重导出 `ModeKey`、`MODES`、`Settings`、`WorkerInboundMessage`、`WorkerOutboundMessage`、`QuickTranslateMessage`、`InitEngineMessage`、`GenerateMessage`、`ResetEngineMessage`、`WorkerUpdateBridgeMessage`。在 `entrypoints/shared/storage.ts` 中把这些键做成常量：`selectedText`、`settings`、`activeTab`、`engineStatus`、`menuIntentMode`、`fetchPageIntent`、`lastProgress`、`autoTriggerAt`、`engineError`、`isLocalModelEnabled`。随后改 `background.ts`、`content.ts`、`offscreen/main.ts`、`useSettings.ts`、`useWorker.ts` 让非 sidepanel 私有逻辑都引用共享契约或共享键名，至少移除 `background.ts` 里本地的 `MODES` 重复定义，并保持现有消息字面量和存储语义完全不变。</action>
  <acceptance_criteria>
    - `entrypoints/shared/contracts.ts` 与 `entrypoints/shared/storage.ts` 存在并导出共享入口。
    - `background.ts` 不再声明本地 `const MODES = [...]`。
    - `background.ts`、`content.ts`、`offscreen/main.ts`、`useSettings.ts`、`useWorker.ts` 中至少能 grep 到对 `./shared/contracts` 或 `../shared/contracts`、`./shared/storage` 或 `../shared/storage` 的引用。
    - 代码里仍保留原始消息名和存储语义，没有擅自改名。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "export .*ModeKey|export .*Settings|selectedText|settings|activeTab|engineStatus|menuIntentMode|fetchPageIntent|lastProgress|autoTriggerAt|engineError|isLocalModelEnabled|shared/contracts|shared/storage" entrypoints/shared/contracts.ts entrypoints/shared/storage.ts entrypoints/background.ts entrypoints/content.ts entrypoints/offscreen/main.ts entrypoints/sidepanel/hooks/useSettings.ts entrypoints/sidepanel/hooks/useWorker.ts && ! rg -n "^const MODES = \\[" entrypoints/background.ts</automated>
  </verify>
  <done>跨上下文共享契约和共享键名有了明确入口，非 sidepanel 代码不再继续复制模式定义和魔法字符串。</done>
</task>

<task type="auto">
  <name>Task 2: 修正 README 的架构、产物目录和测试说明漂移</name>
  <read_first>
    - README.md
    - wxt.config.ts
    - package.json
    - docs/architecture/runtime-context-map.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/TESTING.md
  </read_first>
  <files>README.md</files>
  <action>按 QUAL-03 重写 `README.md` 中会误导维护者的部分，不动商店说明和多语言链接。必须把产物目录从 `.output/` 改成 `dist/`；把开发说明对齐到仓库真实脚本 `npm run dev`、`npm run build`、`npm run compile`、`npm run test`、`npm run test:e2e`；新增一个简短但具体的“Architecture”段，点名 `background.ts`、`content.ts`、`sidepanel/App.tsx`、`offscreen/main.ts`、`sidepanel/worker.ts` 各自职责；在测试说明里明确当前 Playwright 目录是 `tests/e2e`，并提示它并不等同于完整扩展回归。</action>
  <acceptance_criteria>
    - README 中不再出现 `.output/`。
    - README 能 grep 到 `dist/`、`npm run compile`、`npm run test`、`npm run test:e2e`。
    - README 的架构段同时点名 5 个关键入口文件。
    - README 明确说明 `tests/e2e` 的现状边界。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "dist/|npm run compile|npm run test|npm run test:e2e|background.ts|content.ts|sidepanel/App.tsx|offscreen/main.ts|sidepanel/worker.ts|tests/e2e" README.md && ! rg -n "\\.output/" README.md</automated>
  </verify>
  <done>README 对真实构建产物、入口职责和测试入口的描述与仓库一致，不再误导下一阶段执行者。</done>
</task>

</tasks>

<verification>
确保共享契约抽取是“换引用方式”而不是“换语义”；确保 README 说的是当前仓库真实情况而不是理想状态。
</verification>

<success_criteria>
共享契约入口可被多上下文复用，README 与当前代码结构、输出目录和测试入口对齐。
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-and-contracts/01-02-SUMMARY.md`
</output>
