---
phase: 01-extension-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - README.md
  - vitest.config.ts
  - tests/*
  - docs/testing/phase-1-foundation.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-03
must_haves:
  truths:
    - "第 1 阶段有真实可复跑的验证命令。"
    - "新项目的 README 和开发命令能对得上当前仓库。"
    - "最小 smoke 清单已经建立。"
---

<objective>
为新的空仓起点建立最小质量基线，避免后续阶段在没有护栏的情况下推进。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 建立编译与测试基线</name>
  <files>vitest.config.ts, tests/*</files>
  <action>补齐最小测试配置和基础测试样例，至少能验证侧边栏主界面可挂载、模式列表存在、设置默认值存在。命令以 `npm run compile` 和 `npm run test` 为基线。</action>
  <acceptance_criteria>
    - `compile` 命令可执行
    - `test` 命令可执行
    - 至少有最小 UI/设置层测试
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 写清开发说明和 smoke 清单</name>
  <files>README.md, docs/testing/phase-1-foundation.md</files>
  <action>补一份新的 README，说明项目定位、技术栈和开发命令；同时写一份第 1 阶段基础验证文档，记录自动命令和人工 smoke 清单。</action>
  <acceptance_criteria>
    - README 说明当前项目是浏览器扩展
    - README 包含开发与测试命令
    - 验证文档包含最小 smoke 清单
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
第 1 阶段结束时，项目既能开发，也有最小验证和说明文档，不再是裸脚手架。
</success_criteria>

<output>
After completion, create `.planning/phases/01-extension-foundation/01-03-SUMMARY.md`
</output>
